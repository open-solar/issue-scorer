name: Scorer Action
description: Cacluates the score of an issue/pr
inputs:
  priority:
    description: 'The priority of the issue/pr'
    required: true
outputs:
  score:
    value: ${{ steps.plus.outputs.result }}
runs:
  using: node16
  main: dist/index.js
branding:
  icon: bookmark
  color: purple